{
  "name": "xgoose-litellm",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.130.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.18"
  }
}
