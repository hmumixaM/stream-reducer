# Stateless pipeline service deps. Kept lean: no DB/queue/embedding stack —
# the Worker owns D1/Vectorize/R2; this container only fetches + transcribes +
# summarizes and returns JSON.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# [socks] pulls socksio so the cookie-refresh client can egress via the WARP
# SOCKS5 proxy; cryptography provides RSA-OAEP for the refresh correspondPath.
httpx[socks]>=0.28.0
cryptography>=44.0.0
# 2026.08.19 removes the dead android_vr default (its adaptive URLs now always
# 403) and adds the visionOS/HLS fallback. Install the recommended extras so the
# JS challenge solver and curl impersonation handler are present in production.
yt-dlp[default,curl-cffi]==2026.8.19
# yt-dlp plugin that fetches proof-of-origin tokens from the local POT server
# (see Dockerfile/entrypoint.sh). 1.3.2 fixes WebPO generation for the YouTube
# A/B test that made the old provider return HTTP 500.
bgutil-ytdlp-pot-provider==1.3.2
feedparser>=6.0.11
pydantic-settings>=2.6.0
sqlmodel>=0.0.22
opencc-python-reimplemented>=0.1.7
